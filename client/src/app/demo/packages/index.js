import Utils from 'utils';

module.exports = {
  'block-a': Utils.interop(require('./block-a.vue')),
  'block-b': Utils.interop(require('./block-b.vue')),
  'block-c': Utils.interop(require('./block-c.vue'))
}